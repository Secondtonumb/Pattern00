#include <stdio.h>
#include <stdlib.h>

int main(int argc,char* argv[]){
  char *p = argv[1];
  FILE *files = fopen(p, "r");
  char buf[1024];
  int line_num = 0;
  while(fgets(buf, 1024, p)){
    line_num ++;
    }
  fclose(p);
  printf("%d\n", line_num);
  return 0;
}
