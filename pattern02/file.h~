#ifndef
#include <stdio.h>
#include <stdlib.h>

int get_files_line_num(int argc,char* argv[]){
  char *p = argv[1];
  FILE *files = fopen(p, "r");
  char buf[256];
  int line_num = 0;
  while(fgets(buf, 256, files)){
    line_num ++;
    }
  fclose(files);
  return line_num;
}

#endif
